# v0.2.x Docs Hardening Task

Status: active until merged

## Objective

Make `docs/` agent-readable by projecting engineering contracts into features, acceptance cases, registry tables, and ADRs.

## Scope

Docs-only:

- add `docs/AGENTS.md` reading order.
- add coverage matrix.
- add lightweight `plan/`, `features/`, `acceptance-cases/`, `registry/`, `overview/`, and `adr/` files.
- preserve existing README, architecture, tools, development, update, Hermes, and release docs.

Out of scope:

- business logic changes.
- MCP tool changes.
- release tag mutation.
- AstrBot deployment changes.

## Done criteria

- `docs/coverage-matrix.md` maps plan/features/acceptance/code.
- tool contract registry matches `src/mcp.rs` and `tests/mcp_stdio.rs`.
- links and file paths are internally consistent.
- git diff contains documentation files only.
